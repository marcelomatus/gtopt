{
  "// description": [
    "Golden for the SDDP cut-I/O hot-start round-trip case",
    "(sddp_hydro_3phase_hotstart_load).  Derived from golden_iter50.json",
    "— the same converged primal/dual values since loading the converged",
    "cut set into a fresh run and doing a pure sim pass should reproduce",
    "the converged policy exactly — but without the `solver_status` block",
    "because iter=0 (sim-only) runs do NOT emit solver_status.json (no",
    "training iteration, no gap/UB/LB bookkeeping).  The objective check",
    "falls through to `solution.csv` phase=1 obj_value, which pins the",
    "same 323 100 total.",
    "",
    "Reduced pin set (2026-07-08): the fixture's optimum is PRIMAL",
    "DEGENERATE (flat marginal water value — see golden_iter50.json and",
    "golden_iter50_hotstart_clp.json for the full diagnosis), so the",
    "per-phase obj_value tails and per-block dispatch are alternate",
    "optima that drift with solver pivot order.  Pins are restricted to",
    "the solver-invariant quantities: the TOTAL (phase-1 cumulative)",
    "objective, per-phase statuses, the drained horizon efin, and the",
    "phase-1 bus balance dual."
  ],
  "tolerance": {
    "abs": 1.0,
    "rel": 1.0e-4
  },
  "objective_value": 323100.0,
  "// solution_csv_rows": [
    "Phase-1 obj_value is the cumulative total (strict pin).  Phases 2-3",
    "pin status only — their obj_value tails are the degenerate",
    "per-phase split (see description)."
  ],
  "solution_csv_rows": [
    {"scene": 0, "phase": 1, "status": 0, "obj_value": 323100.0},
    {"scene": 0, "phase": 2, "status": 0},
    {"scene": 0, "phase": 3, "status": 0}
  ],
  "element_samples": [
    {
      "// Reservoir drained at the horizon — solver-invariant:": "",
      "class": "Reservoir",
      "stem": "efin_sol",
      "filter": {"scene": 0, "phase": 3, "stage": 3},
      "value_column": "uid:1",
      "expected": 0.0
    },
    {
      "// Bus balance dual = marginal cost = thermal gcost:": "",
      "class": "Bus",
      "stem": "balance_dual",
      "filter": {"scene": 0, "phase": 1, "stage": 1, "block": 1},
      "value_column": "uid:1",
      "expected": 50.0
    }
  ]
}
