/build*
/.vscode
/cpm_modules
.DS_Store
.cache
compile_commands.json
# Exception: tools/compile_commands.json is committed intentionally so agents
# can run clang-tidy without rebuilding (saved by setup_sandbox.sh --build).
!tools/compile_commands.json
CMakeCache.txt
/CMakeFiles
TAGS
*~
.aider*
*.egg-info
__pycache__
test/build/

.python-version

# Coverage reports
coverage-report/
coverage-guiservice/
coverage.info
*.gcda
*.gcno
.coverage
coverage*.xml
htmlcov/

# Web service
webservice/node_modules/
webservice/.next/
webservice/data/

# CodeQL
_codeql_detected_source_root
build/
webservice/tsconfig.tsbuildinfo
webservice/coverage/
output/
LastTest.log
node_modules/
error_*.lp
*.iis.lp
fecha-corrida
plpfact.log
plpwarn.log
userstop
*.deb

# Trace log files (auto-generated by gtopt)
**/logs/trace_*.log

# Solver-side log artifacts produced by ad-hoc gtopt runs (clone diff
# debugging, CPLEX log files dumped at repo root or under support/**/).
clone*.log
cplex.log
**/clone*.log
**/cplex.log
tools/compile_commands.json
# Claude Code: track agent definitions, ignore everything else
.claude/*
!.claude/agents/
# Ignore nested .claude/ scratch dirs anywhere in the tree
**/.claude/
reports
scripts/Testing/
scripts/results/
scripts/Highs.log
scripts/*.log

# Case run outputs (logs and results)
cases/*/logs/
cases/*/results/

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
docs/**/*.out
*.synctex.gz
docs/white_paper/*.log
cases/sddp_hydro_3phase/cuts/

# Regenerated converter output (created on the fly by plexos2gtopt)
support/new_bess_emissions/reference/PLEXOS20260517.json

# gtopt LP-dump artefacts (lp_error / lp_debug), various namings, written to
# the run CWD when solving in the repo root — intermediate, never source.
ERROR*.lp
INFO*.lp
WARN*.lp
WARNING*.lp
debug*.lp
*_scene_*_phase_*.lp
monolithic.lp

# Ad-hoc gtopt run output dirs (per-element results / cuts / solver logs)
# dropped at the repo root or under case dirs.
/logs/
/cuts/
cases/*/out*/

# PSR reference/solution data extracted from the shipped .rar archives
# (comparison inputs, regenerable via unrar — not source, keep out of git).
support/luis/**/psr_ref/
support/luis/**/PDD_DIARIO/
